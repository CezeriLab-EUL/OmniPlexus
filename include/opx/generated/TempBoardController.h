//
// TempBoardController.h
// AUTO-GENERATED BY SmartDrive CommandGenerator - DO NOT EDIT
// Source: TempBoard.json
//

#ifndef SMARTDRIVE_TEMPBOARDCONTROLLER_H
#define SMARTDRIVE_TEMPBOARDCONTROLLER_H

#include "opx/core/platform.h"
#include "opx/core/CommunicationManager.h"
#include "opx/types/ProtocolTypes.h"
#include "CommandTypes.h"

class TempBoardController {
private:
    CommunicationManager& comms;

public:
    static constexpr uint16_t TYPE_ID = 2;

    explicit TempBoardController(CommunicationManager& comms) : comms(comms) {}

    // Set red/green LED state in one command (bitmask)
    bool ledSet(uint8_t state, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::LED_SET;
        cmd.params[0] = state;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn on the red LED
    bool ledRedOn(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::LED_RED_ON;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn off the red LED
    bool ledRedOff(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::LED_RED_OFF;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn on the green LED
    bool ledGreenOn(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::LED_GREEN_ON;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn off the green LED
    bool ledGreenOff(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::LED_GREEN_OFF;
        return comms.dispatch(cmd, transportID, false);
    }

    // Send ID with protocol revision and slave capabilities to STM32
    bool sendId(uint8_t id, uint8_t protocol_revision, uint16_t slave_capabilities, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::SEND_ID;
        cmd.params[0] = id;
        cmd.params[1] = protocol_revision;
        cmd.params[2] = slave_capabilities;
        return comms.dispatch(cmd, transportID, true);
    }

    // Broadcast by master at startup. Slave replies with SEND_ID.
    bool discovery(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::DISCOVERY;
        return comms.dispatch(cmd, transportID, false);
    }

    // --- Telemetry request methods (ON_REQUEST trigger) ---

    // Request current value of: Current temperature reading in centidegrees Celsius (e.g. 2350 = 23.50°C)
    bool getTemperature(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::GET_TEMPERATURE;
        return comms.dispatch(cmd, transportID, false);
    }

    // --- Setting request and set methods ---

    // Get current value of: Telemetry interval for temperature in ms
    bool getTempIntervalMs(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::GET_SETTING_TEMP_INTERVAL_MS;
        return comms.dispatch(cmd, transportID, false);
    }

    // Set value of: Telemetry interval for temperature in ms
    bool setTempIntervalMs(uint16_t value, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::SET_SETTING_TEMP_INTERVAL_MS;
        cmd.params[0] = value;
        return comms.dispatch(cmd, transportID, true);
    }

    // Get current value of: High-temperature threshold in centidegrees (used in auto mode)
    bool getTempThresholdHigh(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::GET_SETTING_TEMP_THRESHOLD_HIGH;
        return comms.dispatch(cmd, transportID, false);
    }

    // Set value of: High-temperature threshold in centidegrees (used in auto mode)
    bool setTempThresholdHigh(int16_t value, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::SET_SETTING_TEMP_THRESHOLD_HIGH;
        cmd.params[0] = value;
        return comms.dispatch(cmd, transportID, true);
    }

    // Get current value of: 0 = manual LED control via commands; 1 = auto (green when below threshold, red when above)
    bool getAutoLedMode(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::GET_SETTING_AUTO_LED_MODE;
        return comms.dispatch(cmd, transportID, false);
    }

    // Set value of: 0 = manual LED control via commands; 1 = auto (green when below threshold, red when above)
    bool setAutoLedMode(uint8_t value, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = TempBoardCommandType::SET_SETTING_AUTO_LED_MODE;
        cmd.params[0] = value;
        return comms.dispatch(cmd, transportID, true);
    }

}; // class TempBoardController

#endif // SMARTDRIVE_TEMPBOARDCONTROLLER_H
