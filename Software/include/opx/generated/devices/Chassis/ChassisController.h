//
// ChassisController.h
// AUTO-GENERATED BY SmartDrive CommandGenerator - DO NOT EDIT
// Source: Chassis.json
//

#ifndef SMARTDRIVE_CHASSISCONTROLLER_H
#define SMARTDRIVE_CHASSISCONTROLLER_H

#include "opx/core/platform.h"
#include "opx/core/CommunicationManager.h"
#include "opx/types/ProtocolTypes.h"
#include "../../shared/CommandTypes.h"

class ChassisController {
private:
    CommunicationManager& comms;

public:
    static constexpr uint16_t TYPE_ID = 3;

    explicit ChassisController(CommunicationManager& comms) : comms(comms) {}

    // Set motor A and B speeds. Positive = forward, negative = backward. Range -255 to 255.
    bool move(int16_t speedA, int16_t speedB, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::MOVE;
        cmd.params[0] = speedA;
        cmd.params[1] = speedB;
        return comms.dispatch(cmd, transportID, false);
    }

    // Stop both motors immediately
    bool stop(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::STOP;
        return comms.dispatch(cmd, transportID, false);
    }

    // Move forward at specified speed
    bool forward(uint8_t speed, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::FORWARD;
        cmd.params[0] = speed;
        return comms.dispatch(cmd, transportID, false);
    }

    // Move backward at specified speed
    bool backward(uint8_t speed, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::BACKWARD;
        cmd.params[0] = speed;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn left at specified speed
    bool turnLeft(uint8_t speed, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::TURN_LEFT;
        cmd.params[0] = speed;
        return comms.dispatch(cmd, transportID, false);
    }

    // Turn right at specified speed
    bool turnRight(uint8_t speed, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::TURN_RIGHT;
        cmd.params[0] = speed;
        return comms.dispatch(cmd, transportID, false);
    }

    // Set individual motor speeds without changing direction
    bool setSpeed(uint8_t speedA, uint8_t speedB, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::SET_SPEED;
        cmd.params[0] = speedA;
        cmd.params[1] = speedB;
        return comms.dispatch(cmd, transportID, false);
    }

    // Send ID with protocol revision and slave capabilities to STM32
    bool sendId(uint8_t id, uint8_t protocol_revision, uint16_t slave_capabilities, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::SEND_ID;
        cmd.params[0] = id;
        cmd.params[1] = protocol_revision;
        cmd.params[2] = slave_capabilities;
        return comms.dispatch(cmd, transportID, true);
    }

    // Broadcast by master at startup. Slave replies with SEND_ID.
    bool discovery(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::DISCOVERY;
        return comms.dispatch(cmd, transportID, false);
    }

    // --- Setting request and set methods ---

    // Get current value of: Maximum PWM duty cycle (0-255)
    bool getMaxSpeed(uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::GET_SETTING_MAX_SPEED;
        return comms.dispatch(cmd, transportID, false);
    }

    // Set value of: Maximum PWM duty cycle (0-255)
    bool setMaxSpeed(uint8_t value, uint8_t transportID = ProtocolConstants::TRANSPORT_ID_DEFAULT) {
        Command cmd;
        cmd.commandType = ChassisCommandType::SET_SETTING_MAX_SPEED;
        cmd.params[0] = value;
        return comms.dispatch(cmd, transportID, true);
    }

}; // class ChassisController

#endif // SMARTDRIVE_CHASSISCONTROLLER_H
